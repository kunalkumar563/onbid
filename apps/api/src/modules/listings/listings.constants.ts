export { CATEGORY_MIN_PRICE, VALID_SUBCATEGORIES, VALID_SUBSUBCATEGORIES } from './category-taxonomy';

export const MIN_PHOTOS = 1;
export const MAX_PHOTOS = 10;

export const MIN_DURATION_HOURS = 1;
export const MAX_DURATION_HOURS = 24 * 14; // 2 weeks — not specified in the PRD, a sane upper bound so a listing can't sit "active" indefinitely
